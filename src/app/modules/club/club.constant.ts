export enum CLUB_ROLE {
  USER = 'user',
  CLUB_MANAGER = 'club_manager',
}
export const clubSearchableField = ['name'];

export enum CLUB_PERIOD_TYPE {
  MINUTE = 'minute',
  HOUR = 'hour',
  DAY = 'day',
}
